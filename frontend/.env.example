# Chép thành .env.local (Vite chỉ đọc file có tiền tố VITE_).
# Ở dev để trống là được — vite.config.ts đã proxy /api sang localhost:8080.
# Khi deploy Vercel, đặt biến này trong dashboard của Vercel.
VITE_API_BASE_URL=
